package me.sanfrancis.util;

import org.bukkit.ChatColor;

/**
 * Created by Max on 28.03.16.
 */
public class Strings {

    public static String PREFIX = new String( ChatColor.DARK_GRAY + "[ BWData ] " + ChatColor.RESET );
    public static String ERROR =  new String( ChatColor.RED + "[ Error ] " + ChatColor.RESET );
    public static String DataFile_Prefix = new String( "Data" );
    private String test = ERROR;
}

